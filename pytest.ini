[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts =
    --strict-markers
    --strict-config
    -ra
    --cov=llm_benchmark
    --cov-report=term-missing
